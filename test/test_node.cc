#include "../src/Node.h"
#include "../src/Graph.h"
namespace rakan {

// Creates a node with no neighbors
Test(Test_Reader, TestNodeCreation) {
}

// Tests to be sure that the Node has an unsigned integer.
TEST(Test_Reader, TestID) {
}

// Creates a node with one neighbors
Test(Test_Reader, TestOneNeighbor) {
}

// Creates a node with many neighbors
Test(Test_Reader, TestManyNeighbors) {
}

// Creates a node with no neighbors
Test(Test_Reader, TestNodeCreation) {
}

// Creates an empty graph. Adds multiple nodes. Checks to see if 

// Creates a demographics map and assigns it to the node's demographic field.
Test(Test_Reader, TestDemographics) {
}
}   // namespace rakan
